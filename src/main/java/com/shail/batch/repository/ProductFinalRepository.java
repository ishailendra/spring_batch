package com.shail.batch.repository;

import com.shail.batch.entity.ProductFinal;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ProductFinalRepository extends JpaRepository<ProductFinal, Integer> {
}
